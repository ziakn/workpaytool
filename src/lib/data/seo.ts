export function pageTitle(title: string) {
  return `${title} | RealSalary`;
}

export function calculatorDescription(calculatorName: string) {
  return `Use RealSalary's ${calculatorName.toLowerCase()} to estimate salary, taxes, rent, cost of living, and real spending power.`;
}
